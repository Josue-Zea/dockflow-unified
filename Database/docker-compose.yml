version: '3'

services:
  cassandra:
    image: cassandra:latest
    container_name: cassandra
    ports:
      - "9042:9042"
      - "7000:7000"
    networks:
      - cassandra-net
    volumes:
      - cassandra-data:/root/cassandra-data

networks:
  cassandra-net:
    driver: bridge

volumes:
  cassandra-data: